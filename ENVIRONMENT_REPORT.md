# Relatório de Ambiente — AP1B Computação Móvel

## 1. Versão do Dart SDK

```
$ dart --version
[COLE AQUI A SAÍDA DE "dart --version"]
```

## 2. Diagnóstico completo do Flutter/Dart Toolchain

```
$ flutter doctor -v
[COLE AQUI A SAÍDA INTEGRAL DE "flutter doctor -v"]
```

> ⚠️ A saída deve evidenciar, sem erros (✗) pendentes:
> - Android SDK configurado corretamente
> - Dart SDK localizado
> - IDE (Android Studio ou VS Code) com plugins Dart/Flutter instalados
> - Ao menos um dispositivo/emulador conectado ou disponível

## 3. Estrutura do repositório Git

- [ ] Repositório inicializado com `git init`
- [ ] `.gitignore` presente na raiz, ignorando `.dart_tool/`, `.packages`,
      `build/` e artefatos temporários de compilação (ver arquivo anexo)
- [ ] Histórico de commits com mensagens descritivas (não apenas
      "commit inicial" / "ajustes")
- [ ] Código formatado com `dart format .` antes de cada commit relevante

## 4. Observações do grupo

[Espaço livre para o grupo registrar particularidades do ambiente:
sistema operacional, versão do Android Studio/VS Code, dispositivo/emulador
usado nos testes, etc.]
